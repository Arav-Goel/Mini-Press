This file is regenerated by `make proof`. Placeholder committed here since
`bun install` / `bun run` were never executed in the environment this
codebase was written in (no Bun runtime available). Run `make proof` on a
real machine with Bun installed before submission — it will produce:

== package.json dependencies ==
  "dependencies": {},
  "devDependencies": {},

== node_modules (should not exist) ==
  ls: cannot access 'node_modules': No such file or directory

== bun.lock / bun.lockb (should not exist) ==
  ls: cannot access 'bun.lock': No such file or directory
  ls: cannot access 'bun.lockb': No such file or directory

That absence of node_modules and any lockfile, alongside the empty
dependencies object above, is the proof: nothing was ever installed.
